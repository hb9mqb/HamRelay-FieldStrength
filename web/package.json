{
  "name": "@hamrelay/field-strength-overlay",
  "version": "0.1.0",
  "description": "Cesium field-strength overlays, dBµV/m legend, and expected S-meter hover sampling",
  "type": "module",
  "license": "MIT",
  "exports": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "peerDependencies": { "cesium": ">=1.120" },
  "devDependencies": { "cesium": "^1.127.0", "typescript": "^5.7.3" }
}
